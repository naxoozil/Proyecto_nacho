<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.nachodelaviuda.proyecto_nacho">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/name_application"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/name_application">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity_register" />
        <activity
            android:name=".MainActivity"
            android:label="@string/name_application"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".NuevoMainActivity"/>
        <activity android:name="NuevoDetallesPelicula"/>
        <activity android:name=".NuevoVisorImagen"/>
        <activity android:name=".Main2Activity"/>
    </application>

</manifest>